<div class="col-sm-6 col-sm-offset-3">
    <% if current_user && current_user.is_admin?%>

<%= form_for(@user) do |f| %>



 <div><%= f.label :name %><br />
        <%= f.text_field :name, autofocus: true , :class => "form-control"%></div>
        

        <%= f.radio_button :is_female, true %> <%= f.label :is_female, "Female" %>
          <%= f.radio_button :is_female, false %> <%= f.label :is_female, "Male" %>
<% if current_user.is_admin? %>
    
        <div><%= f.check_box :is_admin, {}, true %> <%= f.label :is_admin, "Admin" %> </div>
    <div><%= f.check_box :is_packer, {}, true %> <%= f.label :is_packer, "Packer" %> </div>
    <div><%= f.check_box :is_driver, {}, true %> <%= f.label :is_driver, "Driver" %> </div>
    <%end%>
   
    <div><%= f.label :date_of_birth %><br />
        <%= f.date_select :date_of_birth ,:start_year=>Date.today.year - 18, :end_year=>1916%></div>

    <div><%= f.label :email %><br />
        <%= f.email_field :email , :class => "form-control"%></div>
        <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address , :class => "form-control"%></div>
   
    

<div><%= f.submit "Update" , :class => "btn btn-success pull-right"%></div>
 <% end %>
 <% elsif current_user && current_user.id == @user.id%>
 <%= @user.email %><br>
<%= @user.name %><br>
<%= @user.date_of_birth %><br>
<%= @user.is_admin %><br>
<%= @user.is_packer %><br>
<%= form_for(@user) do |f| %>
 <div><%= f.label :name %><br />
        <%= f.text_field :name, autofocus: true %></div>
        

        <%= f.radio_button :is_female, true %> <%= f.label :is_female, "Female" %>
          <%= f.radio_button :is_female, false %> <%= f.label :is_female, "Male" %>
<% if current_user.is_admin? %>
    
        <div><%= f.check_box :is_admin, {}, true %> <%= f.label :is_admin, "Admin" %> </div>
    <div><%= f.check_box :is_packer, {}, true %> <%= f.label :is_packer, "Packer" %> </div>
    <%end%>
   
    <div><%= f.label :date_of_birth %><br />
        <%= f.date_select :date_of_birth ,:start_year=>Date.today.year - 18, :end_year=>1916%></div>

    <div><%= f.label :email %><br />
        <%= f.email_field :email %></div>
        <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %></div>
    <% if current_user.id == @user.id %>

    <%end%>

    

<div><%= f.submit "Update", :class => "btn btn-success pull-right" %></div>

 <% end %>
 <% else %>
 <div class="alert alert-danger">
 <h3>Only Admins can alter other users.</h3>
 <%= link_to "Back to home page", root_path, :method => :get, :class => "btn btn-default"  %>
 </div>
 <%end%>
</div>
